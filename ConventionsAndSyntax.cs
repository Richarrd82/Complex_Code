using UnityEngine;
using System.Collections;

public class NewBehaviourScript : MonoBehaviour {

    //. operator is accessing elements of a compound in Unity
    //compound = something that contains many elements
    //for example transform contains position rotation and scale
    //transform.position.x transform is the country position city and x street

    //unindent shift + tab

    //use comments to disable code
	
<<<<<<< HEAD
    void Start ()
    {
        Debug.Log(transform.position.x);

        if (transform.position.y <= 5f)
        {
            Debug.Log("I am about to hit the ground");
        }
    }
=======
	void Start () 
	{

        	Debug.Log(transform.position.x);

		if (transform.position.y <= 5f) 
        	{
            	Debug.Log("I am about to hit the ground");
		}
	}
>>>>>>> origin/master
}
